# ./github/workflows/docker-publish.yml
name: publish

on:
  push:
    branches:
      - "main" # Production deployment
      - "release/**" # Staging deployment
    tags:
      - "v*" # Version tags (e.g., v1.0.0, v2.1.5)
  workflow_dispatch: # Allow manual triggers

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/${{ github.actor }}/dealership

jobs:
  publish:
    name: Publish Docker Image
    runs-on: ubuntu-latest

    outputs:
      image-tag: ${{ steps.set-tag.outputs.tag }}
      version-tag: ${{ steps.set-tag.outputs.version }}
      environment: ${{ steps.set-tag.outputs.environment }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Fetch all history for tags

      - name: Determine image tag and environment
        id: set-tag
        run: |
          # Check if this is a tag push
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            # Extract version from tag (e.g., v1.0.0 -> 1.0.0)
            VERSION="${GITHUB_REF#refs/tags/v}"
            echo "tag=v$VERSION" >> $GITHUB_OUTPUT
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "environment=release" >> $GITHUB_OUTPUT
            echo "📦 Building tagged release: v$VERSION"

          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            # Get the latest tag for production builds
            LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "0.0.0")
            VERSION="${LATEST_TAG#v}"
            echo "tag=production" >> $GITHUB_OUTPUT
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "environment=production" >> $GITHUB_OUTPUT
            echo "🚀 Building production from tag: $LATEST_TAG"

          elif [[ "${{ github.ref }}" == refs/heads/release/* ]]; then
            # Extract version from branch name (e.g., release/1.0.0 -> 1.0.0)
            VERSION="${GITHUB_REF#refs/heads/release/}"
            echo "tag=staging" >> $GITHUB_OUTPUT
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "environment=staging" >> $GITHUB_OUTPUT
            echo "🧪 Building staging release: $VERSION"

          else
            # Development/other branches
            VERSION="dev-$(git rev-parse --short HEAD)"
            echo "tag=dev" >> $GITHUB_OUTPUT
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "environment=development" >> $GITHUB_OUTPUT
            echo "🔧 Building development: $VERSION"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        run: |
          echo ${{ secrets.PAT }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ steps.set-tag.outputs.tag }}
            ${{ env.IMAGE_NAME }}:v${{ steps.set-tag.outputs.version }}
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache,mode=max

      - name: Output deployment info
        run: |
          echo "🎯 Environment: ${{ steps.set-tag.outputs.environment }}"
          echo "📌 Version: ${{ steps.set-tag.outputs.version }}"
          echo "🏷️  Image Tags:"
          echo "   • ${{ env.IMAGE_NAME }}:${{ steps.set-tag.outputs.tag }}"
          echo "   • ${{ env.IMAGE_NAME }}:v${{ steps.set-tag.outputs.version }}"
          echo "   • ${{ env.IMAGE_NAME }}:${{ github.sha }}"

  deploy-staging:
    needs: publish
    name: Deploy to Staging
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/heads/release/')

    steps:
      - name: Setup SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add Server to Known Hosts
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Pull and Deploy to Staging
        env:
          VERSION: ${{ needs.publish.outputs.version-tag }}
        run: |
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'EOF'
            cd ${{ secrets.WORK_DIR }}

            # Update .env with the versioned tag
            if grep -q "^IMAGE_TAG=" .env; then
              sed -i 's|^IMAGE_TAG=.*|IMAGE_TAG=v${{ needs.publish.outputs.version-tag }}|' .env
            else
              echo "IMAGE_TAG=v${{ needs.publish.outputs.version-tag }}" >> .env
            fi

            echo "🧪 Deploying to STAGING: v${{ needs.publish.outputs.version-tag }}"

            # Pull the versioned image
            docker compose pull dealership-web dealership-queue

            # Recreate containers with new image (including Adminer for staging)
            docker compose --profile staging up -d --force-recreate

            # Show running containers
            docker compose ps

            # Clean up old images (keep last 5 versions)
            docker images ghcr.io/${{ github.actor }}/dealership --format "{{.ID}} {{.Tag}}" | grep -v "buildcache" | tail -n +6 | awk '{print $1}' | xargs -r docker rmi 2>/dev/null || true

            echo "✅ Staging deployment complete: v${{ needs.publish.outputs.version-tag }}"
          EOF

      - name: Cleanup SSH Keys
        run: rm -rf ~/.ssh

  deploy-production:
    needs: publish
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Setup SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PRODUCTION_SSH_KEY }}

      - name: Add Production Server to Known Hosts
        run: ssh-keyscan -H ${{ secrets.PRODUCTION_HOST }} >> ~/.ssh/known_hosts

      - name: Pull and Deploy to Production
        env:
          VERSION: ${{ needs.publish.outputs.version-tag }}
        run: |
          ssh ${{ secrets.PRODUCTION_USER }}@${{ secrets.PRODUCTION_HOST }} << 'EOF'
            cd ${{ secrets.PRODUCTION_WORK_DIR }}

            # Update .env with the versioned tag
            if grep -q "^IMAGE_TAG=" .env; then
              sed -i 's|^IMAGE_TAG=.*|IMAGE_TAG=v${{ needs.publish.outputs.version-tag }}|' .env
            else
              echo "IMAGE_TAG=v${{ needs.publish.outputs.version-tag }}" >> .env
            fi

            echo "🚀 Deploying to PRODUCTION: v${{ needs.publish.outputs.version-tag }}"

            # Pull the versioned image
            docker compose pull dealership-web dealership-queue

            # Recreate containers with new image
            docker compose up -d --force-recreate dealership-web dealership-queue

            # Show running containers
            docker compose ps

            # Clean up old images (keep last 5 versions)
            docker images ghcr.io/${{ github.actor }}/dealership --format "{{.ID}} {{.Tag}}" | grep -v "buildcache" | tail -n +6 | awk '{print $1}' | xargs -r docker rmi 2>/dev/null || true

            echo "✅ Production deployment complete: v${{ needs.publish.outputs.version-tag }}"
          EOF

      - name: Cleanup SSH Keys
        run: rm -rf ~/.ssh
