# .env.docker-desktop - For local development with Docker Desktop

# Docker Configuration
DOCKER_CONTAINER_NAME=dealership
ENVIRONMENT=local # ['local', 'staging', 'production']
BASE_DOMAIN=localhost

# Port mappings for local development
DOCKER_LARAVEL_PORT=8080
DOCKER_POSTGRES_PORT=5432
DOCKER_ADMINER_PORT=8081

# Laravel Application
APP_NAME=Dealership
APP_ENV=local
APP_DEBUG=false
APP_KEY=base64:6Er/R0oRZY/KnspkZrZz3f4ZeH3kvh29kzTcIgQhNKc=
SEED_DATABASE=true

APP_TIMEZONE=Africa/Johannesburg
APP_URL=https://localhost:8080

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration (PostgreSQL for Docker)
DB_CONNECTION=pgsql
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=dealership
DB_USERNAME=postgres
DB_PASSWORD=root

# Typesense Search

# Scout Driver
SCOUT_DRIVER=typesense

# Typesense Connection
TYPESENSE_API_KEY=0b1da32e-b118-4f54-9df5-11254512f01e
TYPESENSE_HOST=dealership-typesense
TYPESENSE_PORT=8108
TYPESENSE_PROTOCOL=http

#Typesense Docker
DOCKER_TYPESENSE_PORT=8108

# Typesense Import Behavior (new - optional)
TYPESENSE_FRESH_IMPORT=false

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
CACHE_PREFIX=null

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=null
AWS_SECRET_ACCESS_KEY=null
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=null
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

GOOGLE_CLIENT_ID=null
GOOGLE_CLIENT_SECRET=null
GOOGLE_REDIRECT_URL=null
FACEBOOK_CLIENT_ID=null
FACEBOOK_CLIENT_SECRET=null
FACEBOOK_REDIRECT_URL=null
